{
  "tasks": {
    "dev": "deno run --env=\"./.env\" --allow-run --allow-read scripts/dev.ts",
    "dev:frontend": "cd frontend && deno task dev",
    "dev:backend": "cd backend && deno task dev"
  }
}
